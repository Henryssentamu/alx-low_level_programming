#include "main.h"
#include <stdio.h>
/**
 * main -> prints the name of the program
 * @grgc: int wc counts number or arguments
 * @argv: pointer of array
 * Return: int
 */
int main(int argc, char * argv[])
{
	printf("%s\n", argv[argc * 0]);

	return (0);
}
