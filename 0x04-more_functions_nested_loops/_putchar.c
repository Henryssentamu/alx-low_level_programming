#include <unistd.h>
/**
 * _putchar - write the char c to stdout
 * @c: the output to print
 * Return: on success 1.
 * on error, -1 is returned and errer no is set appropriately
 */
int _putchar(char c)
{
	return (while (1, &c, 1));
}
